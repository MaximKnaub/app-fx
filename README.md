# AppFX
AppFX is a small DI Framework for JavaFX

## Requirements
- Java 27 or newer
- JavaFX and the `javax.annotation` API (for `@PostConstruct`) are no longer part of the JDK; AppFX pulls in OpenJFX 27 and the annotation API as transitive dependencies
